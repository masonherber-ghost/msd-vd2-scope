-- The notes table was setup-site's end-to-end verification vehicle.
-- /api/scope replaces it. 001_create_notes.sql is already applied and is
-- deliberately left untouched.
DROP TABLE IF EXISTS notes;
